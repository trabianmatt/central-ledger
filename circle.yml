machine:
  node:
    version: 4.0.0

test:
  override:
    - set -o pipefail && npm -s run test:xunit > ${CIRCLE_TEST_REPORTS}/tape.xml && npm -s run test:coverage
  post:
    - mkdir $CIRCLE_ARTIFACTS/coverage
    - mv ./coverage $CIRCLE_ARTIFACTS/coverage